# ============================================================
# 名人书架 (Famous Bookshelf) — 开发环境配置
# ============================================================

spring:
  # -------------------- 数据源 (Supabase PostgreSQL - Dev) --------------------
  datasource:
    url: jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
    username: postgres.umvrczjvvoepmhqliztt
    password: nyKwi6-haxdyd-cakryt
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

# -------------------- MyBatis-Plus (Dev) --------------------
mybatis-plus:
  configuration:
    # SQL 日志输出（仅开发环境开启）
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# -------------------- 日志级别 (Dev) --------------------
logging:
  level:
    root: INFO
    com.famousbookshelf: DEBUG
    com.baomidou.mybatisplus: DEBUG
